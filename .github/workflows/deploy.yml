name: Minify and Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install minification tools
        run: npm install -g uglify-js clean-css-cli

      - name: Create dist directory
        run: mkdir -p dist

      - name: Minify JavaScript files
        run: |
          mkdir -p dist/src/core dist/src/rendering dist/src/strategies dist/src/controllers
          uglifyjs src/core/cube.js -o dist/src/core/cube.js --compress --mangle
          uglifyjs src/rendering/cubeRenderer.js -o dist/src/rendering/cubeRenderer.js --compress --mangle
          uglifyjs src/strategies/rotationStrategy.js -o dist/src/strategies/rotationStrategy.js --compress --mangle
          uglifyjs src/controllers/uiController.js -o dist/src/controllers/uiController.js --compress --mangle
          uglifyjs src/controllers/cameraController.js -o dist/src/controllers/cameraController.js --compress --mangle
          uglifyjs src/rendering/highlightManager.js -o dist/src/rendering/highlightManager.js --compress --mangle
          uglifyjs src/controllers/touchHandler.js -o dist/src/controllers/touchHandler.js --compress --mangle
          uglifyjs src/app.js -o dist/src/app.js --compress --mangle

      - name: Minify CSS
        run: cleancss -o dist/src/styles.css src/styles.css

      - name: Copy HTML and update references
        run: cp index.html dist/index.html

      - name: Copy LICENSE
        run: cp LICENSE dist/LICENSE

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'dist'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
